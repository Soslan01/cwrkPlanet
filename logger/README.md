⚙️ Поведение логгера по конфигу

⸻

🧩 Параметры конфигурации

• Env	string	(Окружение): dev или prod  
• Backend	string	(Бэкенд логирования): std (standard slog) или zap. Если не задан — по умолчанию dev → std, всё остальное → zap.  
• Level	slog.Level	(Явный уровень логирования). Если не задан (0), это соответствует DEBUG.  
• Debug	bool	Упрощённый переключатель уровня в DEBUG (см. правила ниже).  
• AddSource	bool	Добавлять ли информацию об источнике (файл/строка/функция).  
• Service	string	Имя сервиса (добавляется в атрибуты лога).  
• Version	string	Версия сервиса (атрибут лога).  
• InstanceID	string	Идентификатор инстанса (атрибут лога).  
• SampleInitial / SampleThereafter	int	(только для zap) Сэмплинг при всплесках логов. По умолчанию 100 / 10 в секунду.  


⸻  
  
🔍 Правила определения уровня логирования  
  
Одинаковы для std и zap:  
	1.	Если Debug == true и Level == 0 → уровень DEBUG  
	2.	Иначе используется Level  
  
⸻  
  
🧱 Поведение по бэкендам  
  
Backend: std (standard slog handler)  
	•	Формат вывода:  
	•	При Env == prod → JSON  
	•	При Env != prod → TEXT  
	•	Источник (source):  
	•	Управляется строго флагом AddSource: true → добавляется || false → не добавляется (на prod AddSource по умолчанию false)  
  
⸻  
  
Backend: zap (Uber Zap через slog-адаптер)  
	•	Формат вывода: всегда JSON  
	•	Источник (caller): регулируется флагом AddSource: true → добавляется || false → не добавляется (на prod AddSource по умолчанию false)  
	•	Сэмплинг: Также регулируется. По умолчанию SampleInitial = 100/сек & SampleThereafter = 10/сек  
  
⸻  
  
💡 Примеры конфигурации  
  
✅ Dev — стандартный хэндлер, текстовый вывод с источником  
```
logger.Init(logger.Config{
  Service:   "api-gateway",
  Version:   "v0.1.0",
  Env:       logger.EnvDev,
  Backend:   logger.BackendStd, // или не указывать в dev
  Level:     slog.LevelInfo,
  Debug:     true,              // уровень будет DEBUG
  AddSource: true,              // источник добавится
})
```
  
  
⸻  

✅ Prod — zap, JSON, сэмплинг по умолчанию если не указан  
```
logger.Init(logger.Config{
  Service:   "auth-service",
  Version:   "v0.1.0",
  Env:       logger.EnvProd,
  Backend:   logger.BackendZap,
  Level:     slog.LevelInfo,
  Debug:     false,
  AddSource: true,
})
```
